React + Vite frontend for the Connect Four app.

## Run
1. Start the backend: `dotnet run --project src/ConnectFour.Api` (repo root)
2. Start the frontend: `npm run dev` (from this folder)
3. Open http://localhost:5173